kind: Service
apiVersion: v1
metadata:
  name: ambulance-management-webapi
spec:
  selector:
    pod: ambulance-management-webapi-label
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: webapi-port